int removeElement(int* nums, int numsSize, int val) {
    int write_index = 0;
    for (int i = 0; i < numsSize; i++) {
        if (nums[i] != val) {
            nums[write_index] = nums[i];
            write_index++;
        }
    }
    return write_index;
}
